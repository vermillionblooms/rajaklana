comments:
  allowedFields: ["name", "email", "message"]
  branch: "main"
  commitMessage: "New comment added"
  filename: "comment-{@timestamp}"
  format: "yaml"
  moderation: true
  path: "comments"
